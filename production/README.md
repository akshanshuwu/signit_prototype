# SIGNIT Production (Offline, Finals) — PLACEHOLDER

Locked stack: PySide6 + pyqtgraph + Qt-Material Dark + in-process Python (NumPy + pyFFTW + SciPy + Numba + memmap + QThread) + ONNX Runtime + galois + scikit-commpy + pyldpc + PyInstaller single .exe.

Status: independent build for finals. Will be built separately from scratch, optimized for offline (memmap + FFTW + Numba). Do not import from prototype. Duplication allowed.
